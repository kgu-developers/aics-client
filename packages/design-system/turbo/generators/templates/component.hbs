import { cn } from '../../utils';
import { {{kebabCase name}}Variants } from './{{kebabCase name}}.css';

interface Props {
  children: React.ReactNode;
  className?: string;
}

export default function {{pascalCase name}}({ className, children, ...props}: Props) {
  return (
    <div className={cn(txtVariants(), className)} {...props}>
      {children}
    </div>
  );
};