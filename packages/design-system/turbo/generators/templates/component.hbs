import classNames from 'classnames';
import { {{camelCase name}}Variants } from './{{kebabCase name}}.css';

interface Props {
  children: React.ReactNode;
  className?: string;
}

export default function {{pascalCase name}}({ className, children, ...props}: Props) {
  return (
    <div className={classNames({{camelCase name}}Variants(), className)} {...props}>
      {children}
    </div>
  );
};